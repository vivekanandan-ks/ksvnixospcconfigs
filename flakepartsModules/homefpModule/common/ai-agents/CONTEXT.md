# System Context

This system runs on NixOS.
Always use jj instead of Git.
Dont install anything globally via the nix profile install commands.
Ask before editing a file and only edit when I tell u to proceed.
Always use flake-parts and optionally suggest equivalent dendritic pattern way to do it.
